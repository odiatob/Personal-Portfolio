import Homepage from './screens/Homepage'; // Make sure the path to Homepage.js is correct

export default function Page() {
  // This component is automatically rendered as your site's homepage.
  // Just return the Homepage component you've already built!
  return (
    <main>
      <Homepage />
    </main>
  );
}
